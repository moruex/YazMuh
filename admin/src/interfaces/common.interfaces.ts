// src/interfaces/common.interfaces.ts

/** Matches the RoleType enum used in the GraphQL schema and fragments. */
export enum RoleType {
    ACTOR = 'ACTOR',
    DIRECTOR = 'DIRECTOR',
    WRITER = 'WRITER',
    PRODUCER = 'PRODUCER',
    CINEMATOGRAPHER = 'CINEMATOGRAPHER',
    COMPOSER = 'COMPOSER',
}